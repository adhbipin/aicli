[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aicli"
version = "0.1.0"
description = "Local terminal AI powered by Ollama"
requires-python = ">=3.10"
dependencies = ["requests"]

[project.scripts]
aicli = "aicli.main:main"